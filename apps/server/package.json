{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "build": "if [ \"$DEPLOYMENT\" = \"preview\" ]; then convex deploy --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL --cmd 'cd ../web && bunx vite build' --preview-run previewSeed --preview-create $VERCEL_GIT_COMMIT_REF; else convex deploy --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL --cmd 'cd ../web && bunx vite build'; fi",
    "deploy": "bunx --bun convex deploy",
    "deploy:prod": "./scripts/deploy-prod.sh",
    "check-types": "tsc --noEmit || true",
    "test": "echo \"No Convex tests yet\""
  },
  "dependencies": {
    "@convex-dev/better-auth": "^0.10.4",
    "@convex-dev/persistent-text-streaming": "^0.3.0",
    "@convex-dev/rate-limiter": "^0.3.0",
    "better-auth": "1.4.7",
    "convex": "^1.29.3"
  },
  "devDependencies": {
    "convex-test": "^0.0.41"
  }
}
